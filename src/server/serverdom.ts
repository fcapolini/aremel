import { DomNode } from "../shared/dom";
import { HtmlNode } from "./htmldom";

// https://www.typescriptlang.org/docs/handbook/mixins.html

// type GConstructor<T = {}> = new (...args: any[]) => T;
// function ServerNode<TBase extends HtmlNode>(Base: TBase) {
// 	return class ServerNode extends HtmlNode implements DomNode {
// 		nodeType: number;
// 	};
// }
